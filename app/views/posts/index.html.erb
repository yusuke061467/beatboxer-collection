<h1 class = "font-bold text-white">投稿一覧</h1>
  
<div class="font-bold text-white space-y-4 my-4 sm:mt-6">
    <% if @posts.present? %>
        <%= render @posts %>
    <% else %>
        <div class='text-lg text-center'>投稿がありません</div>
    <% end %>
</div>